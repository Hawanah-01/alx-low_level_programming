#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strstr - a function
 * @needle: a parameter
 * @haystack: a parameter
 *
 * Return: values
 */
char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}
