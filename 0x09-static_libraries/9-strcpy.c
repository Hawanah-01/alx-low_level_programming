#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strcpy - a function
 * @dest: a parameter
 * @src: a parameter
 *
 * Return: values
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
