#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isalpha - a function
 * @c: a parameter
 *
 * Return: values
 */
int _isalpha(int c)
{
	return (isalpha(c));
}
